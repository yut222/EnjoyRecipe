<div class="container">
  <div class="row">

    <section class="user-list">
      <h3 class="cooking-title mb-4"><%= @title %></h3>

      <% if @users.any? %>
        <%= render 'shared/user-list' %>
      <% else %>
        <p class="notthing"><%= @message %></p>
      <% end %>

      <div class="col-md-12 justify-content-center d-flex mb-4">
     　 <%= paginate @users %>
    　</div>


    　　<%= link_to "マイページに戻る", @user, class: "withdrawal btn btn-outline-primary mb-4" %>

    </section>

  </div>
</div>