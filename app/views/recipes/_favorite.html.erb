<section class="favorite text-right mb-2">
  <% if user_signed_in? %>
    <% if current_user.already_favorited?(recipe) %>  <!--いいね数-->
      <%= link_to recipe_favorites_path(recipe), remote: true, method: :delete do %>
        <i class="fas fa-heart"></i>
      <% end %>
      <span class="mr-2"><%= recipe.favorites.length %></span>
    <% else %>
      <%= link_to recipe_favorites_path(recipe), remote: true, method: :post do %>
        <i class="far fa-heart"></i>
      <% end %>
      <span class="mr-2"><%= recipe.favorites.length %></span>
    <% end %>
  <% end %>

  <%= link_to user_path(id: recipe.user_id) do %>
    <%= image_tag recipe.user.avatar.url, class:'list__user-img rounded-circle' %>  <!--ユーザ画像-->
  <% end %>
  <%= link_to recipe.user.name, user_path(id: recipe.user_id), class: "mr-2" %>  <!--ユーザ名-->
</section>