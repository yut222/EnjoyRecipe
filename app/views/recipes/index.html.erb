<%= render 'shared/flash' %>

<div class="container">
  <div class="row">
    <div class="px-sm-0 mt-4">
      <h3>レシピ一覧</h3>
    </div>
    <div class="col-md-12 px-sm-0 mt-2">
      <%= render 'shared/recipe_search' %>
      <%= render 'shared/recipe_tag_list' %>
      <%= render 'shared/recipe_list' %>
    </div>
  </div>
  <div class="col-md-12 justify-content-center d-flex mt-4 mb-4">
    <%= paginate @recipes %>
  </div>
</div>